Go ahead with implementation.
Note, For security reasons, we don't want to execute code directly on the server. Instead, let's use WASM to execute the code only in the browser.
Use best practices to organize code in the folders. 
Include in the readme the description of the folder structure.
Don't forget tests and include in the separate .md the instructions how to run tests.
Finally dockerize all and provide insructions how to run all thing. 
run both client and server at the same time. Use concurrently for that. (What's the command you have in package.json for npm dev for running both?). Provide instruction to run this and 
Put both backend and frontend in one container.